# Integration

Minimal .NET debuggee projects used to exercise `csdbg` features during development.

Current project:

- `DebuggableProject`: small console app with stable breakpoint targets for launch, attach, loop, and exception scenarios.
